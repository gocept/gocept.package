[buildout]
extends =
    http://download.zope.org/zopetoolkit/index/1.1.3/ztk-versions.cfg
parts = doc
allow-picked-versions = false

[versions]
gocept.package = {{gocept_package_version}}

[doc]
recipe = zc.recipe.egg
eggs = gocept.package [doc]
